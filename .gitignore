# Compiled binaries (build outputs — never commit; rebuild with `make`/`go build`)
/hzctl
/hzd
/transformer
/hz-drops
/hz-drophost
/ha_loadtest
/.hazyflow/
*.xlsx
/.env

/node_modules/
# Generated SBOM artifacts (produced by the CI `sbom` task; attached to releases)
/sbom.spdx.json
/sbom.cdx.json
# Local key material (legacy hz-drops signing keys) — never commit
.keys/
# Hazy personal API key used by the complete-hazyflow-tasks skill — never commit
hazy-key
# Local Claude Code state stays out of git, but the shared skills under
# .claude/skills/ are committed so the team gets them.
/.claude/*
!/.claude/skills/
/.mcp.json
