# Compiled binaries (build outputs — never commit; rebuild with `make`/`go build`)
/hzctl
/hzd
/transformer
/hz-drops
/hz-drophost
/ha_loadtest
/.hazyflow/
*.xlsx
/.env

/node_modules/
# Local key material (legacy hz-drops signing keys) — never commit
.keys/
/.claude
